# Copy this file to `.env` and fill in the credentials for your MySQL instance.
# The configuration loader in config.php reads both Server (API and admin)/.env
# and the repository root ./.env file.
#
# ⚠️ On shared hosts that expose `.env` files over the web, prefer using the
# PHP-based config.local.php approach instead (see config.local.example.php).
DB_HOST=127.0.0.1
DB_NAME=beavr
DB_USER=beavr
DB_PASS=secret
